doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Reset Your Password
    style.
      body {
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
        padding: 20px;
      }
      .container {
        max-width: 500px;
        margin: 0 auto;
        background: #ffffff;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }
      h1 {
        font-size: 22px;
        margin-bottom: 10px;
        color: #333333;
      }
      p {
        font-size: 16px;
        line-height: 1.5;
        color: #555555;
      }
      .btn-reset {
        display: inline-block;
        margin: 20px 0;
        padding: 12px 20px;
        background-color: #008cff;
        color: #ffffff !important;
        text-decoration: none;
        font-size: 16px;
        font-weight: bold;
        border-radius: 8px;
        text-align: center;
      }
      .expire {
        font-size: 14px;
        color: #777;
        margin-top: 10px;
      }
  body
    .container
      h1 Hello #{name},
      p You requested to reset your password.
      p Click the button below to create a new password:

      a.btn-reset(href=resetUrl) Reset Your Password

      p.expire This link will expire in #{expiresIn} minutes.
      p If you didn't request this reset, please ignore this email.
      p Regards,
      p strong Trello Team
